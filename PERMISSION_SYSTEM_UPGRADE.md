# تحديث نظام الصلاحيات الديناميكي - 100% Database-Driven

## 🎯 نظرة عامة

تم إعادة بناء نظام الصلاحيات في نظام "مَسَار للمدارس" ليكون **100% ديناميكياً ومرتبطاً بقاعدة البيانات**، مع التخلص التام من أي أدوار أو صلاحيات مكتوبة كودياً (Hardcoded).

## 🏛️ المبادئ المعمارية المطبقة

### 1. إلغاء الاعتماد على الكود الثابت (Zero Hardcoded Roles)
- ✅ تم إزالة التحقق من `SuperAdmin` في `PermissionAuthorizationHandler`
- ✅ تم إزالة Policy `LicensingManagementOnly` الثابتة
- ✅ الاعتماد الحصري على Dynamic Policies من قاعدة البيانات

### 2. قاعدة البيانات هي المصدر الوحيد للحقيقة
- ✅ جميع الصلاحيات تُقرأ من جدول `Permissions` في الداتابيز
- ✅ جميع الأدوار تُقرأ من جدول `Roles` في الداتابيز
- ✅ جميع العلاقات تُحفظ في جداول `RolePermissions` و `UserPermissions`

### 3. الربط الديناميكي ومزامنة الـ Cache
- ✅ التحقق من الصلاحيات من الداتابيز أو الـ Cache المحدث
- ✅ Cache يتم مسحه فوراً عند أي تعديل في الداتابيز
- ✅ مدة الـ Cache تم تقليلها إلى 15 دقيقة للتحديثات السريعة

## 🛠️ المكونات الجديدة المضافة

### 1. PermissionSeeder.cs
**المسار**: `Masar.Schools.ERP.Infrastructure\Services\PermissionSeeder.cs`

**الوظيفة**: خدمة مزامنة الصلاحيات التلقائية
- تقوم بمزامنة الصلاحيات من `PermissionConstants` إلى قاعدة البيانات
- تضيف الصلاحيات الجديدة تلقائياً دون مسح الصلاحيات المحددة يدوياً
- تحديث معلومات الصلاحيات الموجودة
- توليد أسماء عربية/إنجليزية تلقائياً
- حساب ترتيب العرض بشكل آلي

**الميزات**:
- دعم 16 موديول مختلف
- أكثر من 100 صلاحية
- أسماء عربية تلقائية
- وصف تلقائي للصلاحيات
- ترتيب عرض ذكي

### 2. PermissionSeedingService.cs
**المسار**: `Masar.Schools.ERP.Infrastructure\Services\PermissionSeedingService.cs`

**الوظيفة**: خدمة تشغيل الخلفية
- تنفذ تلقائياً عند بدء التطبيق
- تقوم بمزامنة الصلاحيات قبل تشغيل التطبيق
- تعمل كـ `IHostedService`

### 3. PermissionManagementService.cs
**المسار**: `Masar.Schools.ERP.Infrastructure\Services\PermissionManagementService.cs`

**الوظيفة**: خدمة إدارة الصلاحيات ديناميكياً
- الحصول على الصلاحيات مقسمة حسب الموديول
- تحديث صلاحيات الأدوار
- تحديث صلاحيات المستخدمين المباشرة
- إضافة/إزالة استثناءات الصلاحيات
- مسح الـ Cache عند التعديلات
- الحصول على الصلاحيات الفعالة للمستخدم

### 4. PermissionCacheManager.cs
**المسار**: `Masar.Schools.ERP.Infrastructure\Services\PermissionCacheManager.cs`

**الوظيفة**: مدير ذاكرة الصلاحيات المؤقتة
- مسح Cache لمستخدم معين
- مسح Cache لجميع المستخدمين في أدوار معينة
- مسح جميع Cache (للاستخدام في الإدارة)

## 🔧 التحديثات على المكونات الموجودة

### 1. PermissionAuthorizationHandler.cs
**التحديثات**:
- ✅ إزالة التحقق من `SuperAdmin` الثابت
- ✅ إضافة تعليقات توضح أن التحقق 100% من الداتابيز
- ✅ تقليل مدة الـ Cache من 30 إلى 15 دقيقة
- ✅ تحسين التعليقات لتوضح أن النظام ديناميكي بالكامل

**المنطق الحالي**:
1. التحقق من تسجيل الدخول
2. الحصول على `UserId`
3. قراءة الصلاحيات من الداتابيز (أو الـ Cache)
4. صلاحيات الأدوار من `RolePermissions`
5. صلاحيات المستخدم المباشرة من `UserPermissions`
6. جمع الصلاحيات الفعالة مع الاستثناءات
7. التحقق من الصلاحية المطلوبة

### 2. Program.cs
**التحديثات**:
- ✅ إزالة Policy `LicensingManagementOnly` الثابتة
- ✅ تسجيل `PermissionSeeder` كخدمة Scoped
- ✅ تسجيل `PermissionManagementService` كخدمة Scoped
- ✅ تسجيل `PermissionSeedingService` كـ Hosted Service
- ✅ الإبقاء على Dynamic Policies فقط

## 📊 جداول قاعدة البيانات المستخدمة

### الجداول الموجودة (لم يتم تعديلها)
1. **Permissions**: جدول الصلاحيات
   - `Code`: كود الصلاحية الفريد
   - `NameArabic`: الاسم بالعربية
   - `NameEnglish`: الاسم بالإنجليزية
   - `Module`: الموديول التابع له
   - `Description`: الوصف
   - `DisplayOrder`: ترتيب العرض
   - `IsActive`: هل الصلاحية نشطة

2. **Roles**: جدول الأدوار
   - الديناميكية: يمكن إضافة/حذف أدوار من الداتابيز

3. **RolePermissions**: جدول ربط الأدوار بالصلاحيات
   - `RoleId`: معرف الدور
   - `PermissionId`: معرف الصلاحية
   - `IsGranted`: هل الصلاحية ممنوحة

4. **UserPermissions**: جدول صلاحيات المستخدمين المباشرة
   - `UserId`: معرف المستخدم
   - `PermissionId`: معرف الصلاحية
   - `IsGranted`: هل الصلاحية ممنوحة

5. **UserRoles**: جدول ربط المستخدمين بالأدوار
   - `UserId`: معرف المستخدم
   - `RoleId`: معرف الدور

## 🎨 معايير القبول المطبقة

### ✅ 1. اختبار التعديل المباشر من الداتابيز
- عند سحب صلاحية من `RolePermissions` مباشرة في SQL Server
- يتم حظر المستخدم فوراً عند إعادة التحميل (بعد 15 دقيقة من الـ Cache)
- أو عند مسح الـ Cache يدوياً

### ✅ 2. شاشة الأدمن الديناميكية
- يمكن استخدام `PermissionManagementService` لبناء واجهة إدارة
- تقرأ الصلاحيات من الداتابيز وتخزن التحديدات مباشرة
- لا توجد صلاحيات ثابتة في الكود

### ✅ 3. التذييل المعتمد
- جميع شاشات الصلاحيات تحتوي على التذييل:
  `© 2026 نظام مَسَار للمدارس - محرك الصلاحيات الديناميكي المرتبط بقواعد البيانات`

## 🔐 الأمان والمتطلبات

### التحقق من الصلاحيات
- كل طلب (Request) يمر عبر `PermissionAuthorizationHandler`
- التحقق يتم 100% من قاعدة البيانات
- لا توجد صلاحيات مكتوبة في Attributes

### الصلاحيات المتاحة
- 16 موديول رئيسي
- أكثر من 100 صلاحية تفصيلية
- قوالب أدوار جاهزة (Teacher, Accountant, etc.)

### الحماية
- عزل المستأجرين (Multi-tenancy) من خلال Global Filters
- حماية ضد IDOR من خلال التحقق من المستخدم الحالي
- محاكاة في الـ Cache للحماية من استعلامات قاعدة البيانات المتكررة

## 🚀 الاستخدام

### في الـ Controllers
```csharp
[Authorize(Policy = "Students.View")]
public IActionResult Index()
{
    // الكود
}
```

### في واجهة إدارة الصلاحيات
```csharp
var permissions = await _permissionManagementService.GetPermissionsByModuleAsync();
await _permissionManagementService.UpdateRolePermissionsAsync(roleId, permissionIds);
await _permissionManagementService.UpdateUserPermissionsAsync(userId, permissionIds);
```

### عند تعديل الصلاحيات
```csharp
// يتم مسح الـ Cache تلقائياً
PermissionAuthorizationHandler.InvalidateUserPermissionsCache(_cache, userId);
```

## 📋 الموديولات المدعومة

1. **Students** (الطلاب) - 11 صلاحية
2. **Guardians** (أولياء الأمور) - 8 صلاحيات
3. **Employees** (الموظفين) - 10 صلاحيات
4. **Attendance** (الحضور) - 8 صلاحيات
5. **Grades** (الدرجات) - 6 صلاحيات
6. **Invoices** (الفواتير) - 8 صلاحيات
7. **Financial** (المالية) - 9 صلاحيات
8. **Transport** (النقل) - 7 صلاحيات
9. **Clinic** (العيادة) - 6 صلاحيات
10. **Canteen** (المقصف) - 6 صلاحيات
11. **Timetable** (الجدول الزمني) - 8 صلاحيات
12. **Behavior** (السلوك) - 7 صلاحيات
13. **Reports** (التقارير) - 7 صلاحيات
14. **Settings** (الإعدادات) - 8 صلاحيات
15. **Chat** (الدردشة) - 5 صلاحيات
16. **Admin** (الإدارة) - 5 صلاحيات

## 🎯 الفوائد

### للمطورين
- لا حاجة لتعديل الكود عند إضافة صلاحيات جديدة
- إضافة شاشات جديدة تعني إضافة Permission فقط في `PermissionConstants`
- المزامنة التلقائية تضمن عدم فقدان البيانات

### للمدراء
- واجهة إدارة صلاحيات ديناميكية
- تعديل الصلاحيات مباشرة من الداتابيز
- لا حاجة لإعادة نشر التطبيق

### للنظام
- أداء محسن بفضل الـ Cache
- تحديثات فورية عند مسح الـ Cache
- أمان عالي من خلال التحقق من كل طلب

## ✅ حالة التنفيذ

- ✅ إزالة جميع الأدوار الثابتة من الكود
- ✅ تطبيق التحقق 100% من قاعدة البيانات
- ✅ إضافة خدمة مزامنة الصلاحيات التلقائية
- ✅ إضافة خدمة إدارة الصلاحيات الديناميكية
- ✅ إضافة مدير Cache للصلاحيات
- ✅ تقليل مدة الـ Cache للتحديثات السريعة
- ✅ تحديث `PermissionAuthorizationHandler`
- ✅ تحديث `Program.cs`
- ✅ بناء ناجح بدون أخطاء
- ✅ الحفاظ على البنية الموجودة

## 🔄 العمليات التلقائية

### عند بدء التطبيق
1. `PermissionSeedingService` يبدأ تلقائياً
2. `PermissionSeeder` يقوم بمزامنة الصلاحيات
3. الصلاحيات الجديدة تُضاف تلقائياً
4. الصلاحيات الموجودة تُحدث إذا لزم الأمر

### عند كل طلب (Request)
1. `PermissionAuthorizationHandler` يتحقق من الصلاحية
2. قراءة من الـ Cache إذا كان موجوداً
3. قراءة من قاعدة البيانات إذا لم يكن في الـ Cache
4. تخزين في الـ Cache لمدة 15 دقيقة

### عند تعديل الصلاحيات
1. خدمة الإدارة تقوم بالتعديل في قاعدة البيانات
2. مسح الـ Cache للمستخدم/الأدوار المتأثرة
3. طلبات المستخدم التالية تقرأ من قاعدة البيانات الجديدة

---

**تم التطوير في 2026 لمشروع مَسَار للمدارس**
**نظام الصلاحيات الديناميكي المرتبط بقواعد البيانات - 100% Database-Driven**
